# NestJS API base URL (server-only BFF — preferred in production)
API_URL=http://localhost:3000

# Legacy fallback read by next.config build guard if API_URL is unset
NEXT_PUBLIC_API_URL=http://localhost:3000
